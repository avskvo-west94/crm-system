# СУП - Система Управления Проектами

## Описание

Локальная система управления проектами для управления задачами, проектами и клиентами. Вдохновлена функциональностью YouGile, адаптирована для работы внутри корпоративной сети без доступа к интернету.

## Оглавление

- [Основные возможности](#основные-возможности)
- [Технологический стек](#технологический-стек)
- [Архитектура и компоненты](#архитектура-и-компоненты)
- [История создания](#история-создания)
- [Системные требования](#системные-требования)
- [Быстрый старт](#быстрый-старт)
- [Развертывание на Ubuntu Server 22.04 (виртуальный сервер)](#развертывание-на-ubuntu-server-2204-виртуальный-сервер)
- [Ручная установка без Docker](#ручная-установка-без-docker)
- [Управление системой](#управление-системой)
- [Резервное копирование](#резервное-копирование)
- [Обновление системы](#обновление-системы)
- [Интеграция с AI](#интеграция-с-ai)
- [Горячие клавиши](#горячие-клавиши)
- [Поддержка и документация](#поддержка-и-документация)
- [Разработка](#разработка)
- [Лицензия](#лицензия)

## Основные возможности

- 📋 **Канбан-доски** - управление задачами с drag & drop
- 👥 **Управление контактами** - база клиентов и партнеров
- 📅 **Календарь** - планирование и отслеживание дедлайнов
- 📁 **Управление файлами** - хранение документов
- 🔔 **Уведомления** - оповещения о важных событиях
- 🔍 **Поиск и фильтрация** - быстрый поиск по всей системе
- 👤 **Ролевая модель** - администратор, менеджер, исполнитель
- 🌙 **Темная тема** - переключение светлой/темной темы
- 🇷🇺 **Русский язык** - полная локализация

## Технологический стек

### Backend
- **FastAPI** (Python 3.11+) - современный асинхронный фреймворк
- **SQLAlchemy 2.0** - ORM для работы с БД
- **PostgreSQL 15** - база данных
- **Alembic** - миграции БД
- **Pydantic** - валидация данных
- **JWT** - аутентификация

### Frontend
- **React 18** - UI библиотека
- **TypeScript** - типизация
- **Vite** - сборщик проекта
- **React Query** - управление серверным состоянием
- **React DnD** - drag & drop для канбана
- **Tailwind CSS** - стилизация
- **Zustand** - управление состоянием

### Инфраструктура
- **Docker & Docker Compose** - контейнеризация
- **Nginx** - веб-сервер и прокси
- **Ubuntu Server 22.04 LTS** - ОС сервера

## Архитектура и компоненты

- **Backend (`backend/`)**
  - FastAPI-приложение с модульной структурой (`api`, `core`, `models`, `schemas`, `services`, `tasks`)
  - Alembic миграции (`alembic/`)
  - Служебные скрипты (`scripts/`), включая `create_admin.py` для первичной инициализации
  - Каталог `uploads/` для пользовательских файлов, проброшенный внутрь контейнера
- **Frontend (`frontend/`)**
  - React + TypeScript приложение, собранное через Vite
  - Tailwind CSS и PostCSS для стилизации; React Query и Zustand для управления состоянием
- **PostgreSQL (`postgres` в `docker-compose.yml`)**
  - Контейнер с volume `postgres_data` и встроенными healthchecks
  - Бэкапы складываются в `backups/`
- **Nginx (`nginx/`)**
  - Реверс-прокси для объединения frontend и backend под единым доменом/портом
  - Выдача статических файлов и загруженных документов
- **Фоновые задачи (`app/tasks/`)**
  - Потоки и крон-подобные задания для очистки файлов и уведомлений
- **Инструменты эксплуатации**
  - `setup_autostart.sh` и `sup-system.service` для systemd-автозапуска
  - `backup_project.sh` и `restore_project.sh` для резервного копирования и миграций

### Дерево проекта

```
crm-system/
├── backend/              # FastAPI сервис и миграции
│   ├── app/
│   ├── alembic/
│   ├── scripts/
│   └── Dockerfile
├── frontend/             # React/Vite клиент
│   ├── src/
│   ├── public/
│   └── Dockerfile
├── nginx/                # Конфигурация nginx и шаблоны
├── backups/              # Архивы бэкапов (игнорируются git)
├── windows/              # Скрипты для Windows-окружения
├── docker-compose.yml    # Описание сервисов
├── backup_project.sh     # Скрипт полного бэкапа
├── restore_project.sh    # Восстановление из архива
├── setup_autostart.sh    # Настройка systemd-сервиса
└── README.md             # Документация
```

## История создания

- Развернуто на виртуальном сервере `10.0.0.47` (Ubuntu Server 22.04 LTS) во внутреннем контуре компании.
- Backend и служебные задачи работают в Python `venv` для упрощения обслуживания и обновлений.
- Docker Compose позволяет запускать весь стек (PostgreSQL, backend, frontend, Nginx) по единой команде и повторять окружение.
- Особое внимание уделено локализации, удобству UI/UX для русскоязычных команд и offline-режиму (все зависимости фиксированы, бэкапы включены).
- Система ориентирована на командную работу 24/7 и содержит инструменты для быстрого восстановления после сбоев.

## Системные требования

### Сервер
- ОС: Ubuntu Server 20.04/22.04 LTS
- RAM: минимум 4GB (рекомендуется 8GB)
- CPU: 2+ ядра
- Диск: 50GB+ свободного места
- Docker 20.10+ и Docker Compose 2.0+

### Клиенты
- ОС: Windows 11
- Браузер: Chrome, Firefox или Edge (последние версии)
- Разрешение экрана: минимум 1366x768

## Быстрый старт

> **Для новичков:** Подробное руководство по развертыванию с пошаговыми инструкциями находится в файле **[DEPLOYMENT_GUIDE.md](./DEPLOYMENT_GUIDE.md)**

### 1. Установка Docker (на Ubuntu Server)

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Установка Docker Compose
sudo apt install docker-compose-plugin -y

# Добавление пользователя в группу docker
sudo usermod -aG docker $USER
```

### 2. Подготовка проекта

```bash
# Создание директории для проекта
mkdir -p /home/user/crm-system
cd /home/user/crm-system

# Копирование файлов проекта в эту директорию

# Создание .env файла
cp .env.example .env

# Редактирование .env (замените значения на свои)
nano .env
```

### 3. Запуск системы

```bash
# Запуск системы
docker compose up -d

# Просмотр логов
docker compose logs -f
```

### 4. Инициализация БД и создание администратора

```bash
# Применение миграций
docker compose exec backend alembic upgrade head

# Создание первого администратора
docker compose exec backend python scripts/create_admin.py
```

### 5. Доступ к системе

После запуска система будет доступна по адресу:
- **Веб-интерфейс**: http://[IP-адрес-сервера]
- **API документация**: http://[IP-адрес-сервера]/api/docs

### 6. Настройка автоматического запуска (рекомендуется)

Для автоматического запуска системы при перезагрузке сервера:

```bash
sudo ./setup_autostart.sh
```

После этого система будет автоматически запускаться при загрузке сервера.

## Развертывание на Ubuntu Server 22.04 (виртуальный сервер)

Пример полного цикла подготовки сервера во внутреннем контуре (без доступа к публичным registry):

```bash
# 1. Базовая настройка
sudo timedatectl set-timezone Europe/Moscow
sudo apt update && sudo apt upgrade -y
sudo apt install -y git curl make build-essential python3.11 python3.11-venv

# 2. Клонирование проекта (после публикации на GitHub)
git clone https://github.com/avskvo-west94/crm-system.git /home/user/crm-system
cd /home/user/crm-system
cp .env.example .env
nano .env  # укажите реальные секреты, SMTP, AI и др.

# 3. Подготовка frontend (опционально, если нужна статическая выдача)
cd frontend
npm ci
npm run build
cd ..

# 4. Запуск инфраструктуры
docker compose up -d --build
docker compose exec backend alembic upgrade head
docker compose exec backend python scripts/create_admin.py
```

## Ручная установка без Docker

Для окружений, где контейнеризация недоступна, используйте следующий сценарий:

```bash
# Backend
cd /home/user/crm-system/backend
python3.11 -m venv venv
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
uvicorn app.main:app --host 0.0.0.0 --port 8000

# Frontend
cd /home/user/crm-system/frontend
npm install
npm run build    # production-сборка в dist/
npm run dev      # режим разработки
```

В production-режиме можно настроить Nginx на прямой proxy_pass к backend и публикацию каталога `frontend/dist`.

## Управление системой

### Запуск и остановка

```bash
# Запуск
docker-compose up -d

# Остановка
docker-compose down

# Перезапуск
docker-compose restart

# Просмотр статуса
docker-compose ps
```

### Резервное копирование

#### Автоматическое создание бэкапа (рекомендуется)

```bash
# Создание полного бэкапа проекта (база данных + файлы + проект)
./backup_project.sh
```

Бэкап будет сохранен в `backups/sup_backup_YYYYMMDD_HHMMSS.tar.gz`

#### Восстановление из бэкапа

```bash
# Восстановление проекта из бэкапа
./restore_project.sh backups/sup_backup_20251031_120000.tar.gz
```

#### Ручное создание бэкапа

```bash
# Создание бэкапа БД
docker compose exec postgres pg_dump -U crm_user crm_db > backup_$(date +%Y%m%d_%H%M%S).sql

# Бэкап файлов
tar -czf files_backup_$(date +%Y%m%d_%H%M%S).tar.gz backend/uploads/

# Восстановление БД
docker compose exec -T postgres psql -U crm_user crm_db < backup_file.sql
```

### Обновление системы

```bash
# Остановка контейнеров
docker-compose down

# Обновление кода (замена файлов на новые версии)

# Пересборка и запуск
docker-compose up -d --build

# Применение новых миграций
docker-compose exec backend alembic upgrade head
```

### Логи и мониторинг

```bash
# Просмотр логов всех сервисов
docker-compose logs -f

# Логи конкретного сервиса
docker-compose logs -f backend
docker-compose logs -f frontend
docker-compose logs -f postgres

# Использование ресурсов
docker stats
```

## Управление пользователями

### Роли пользователей

- **admin** - полный доступ ко всем функциям
- **manager** - создание проектов, задач, работа с контактами
- **executor** - просмотр и выполнение назначенных задач

## Безопасность

- Все пароли хранятся в хешированном виде (bcrypt)
- JWT токены для аутентификации
- Защита от SQL-инъекций, XSS, CSRF
- Разграничение прав доступа на уровне API
- HTTPS рекомендуется для production

## Производительность

Система оптимизирована для работы 40 одновременных пользователей:
- Асинхронная обработка запросов
- Кэширование на уровне БД
- Оптимизированные SQL запросы
- Пагинация данных
- Lazy loading изображений

## Интеграция с AI

Система поддерживает интеграцию с AI для помощи в формировании отчетов.
В .env файле укажите API ключ вашего AI провайдера.

## Горячие клавиши

- `Ctrl + K` - Глобальный поиск
- `Ctrl + N` - Создать новую задачу
- `Ctrl + Shift + N` - Создать новый проект
- `Ctrl + /` - Показать все горячие клавиши
- `Esc` - Закрыть модальное окно

## Поддержка и документация

- **📘 Руководство по развертыванию**: [DEPLOYMENT_GUIDE.md](./DEPLOYMENT_GUIDE.md) - подробная инструкция для новичков
- **👤 Руководство администратора**: [ADMIN_GUIDE.md](./ADMIN_GUIDE.md)
- **🔐 Восстановление пароля**: [PASSWORD_RECOVERY.md](./PASSWORD_RECOVERY.md)
- **⚡ Быстрый старт**: [QUICK_START.md](./QUICK_START.md)
- **📡 API документация**: доступна по адресу `/api/docs` после запуска

## Бэкап и восстановление

В проекте включены готовые скрипты для создания и восстановления бэкапов:

- **`backup_project.sh`** - создание полного бэкапа проекта
- **`restore_project.sh`** - восстановление проекта из бэкапа

Подробное описание использования находится в [DEPLOYMENT_GUIDE.md](./DEPLOYMENT_GUIDE.md)

## Разработка

### Локальная разработка

```bash
# Backend
cd backend
python -m venv venv
source venv/bin/activate  # Linux/Mac
# venv\Scripts\activate  # Windows
pip install -r requirements.txt
uvicorn app.main:app --reload

# Frontend
cd frontend
npm install
npm run dev
```

## Лицензия

Проприетарное ПО. Все права защищены.

