# 📘 РУКОВОДСТВО ПО РАЗВЕРТЫВАНИЮ СУП
## Система Управления Проектами

> **Для новичков в сфере ИТ**  
> Это руководство поможет вам самостоятельно развернуть систему СУП на сервере, даже если вы не имеете опыта работы с Linux или Docker.

---

## 📋 СОДЕРЖАНИЕ

1. [Требования к серверу](#требования-к-серверу)
2. [Подготовка сервера](#подготовка-сервера)
3. [Установка Docker](#установка-docker)
4. [Развертывание проекта](#развертывание-проекта)
5. [Первоначальная настройка](#первоначальная-настройка)
6. [Повседневные команды](#повседневные-команды)
7. [Решение проблем](#решение-проблем)
8. [Бэкап и восстановление](#бэкап-и-восстановление)

---

## 🖥️ ТРЕБОВАНИЯ К СЕРВЕРУ

### Минимальные требования:
- **Операционная система:** Ubuntu Server 20.04 LTS или 22.04 LTS
- **Оперативная память (RAM):** минимум 4 ГБ (рекомендуется 8 ГБ)
- **Процессор:** 2 ядра (рекомендуется 4 ядра)
- **Жесткий диск:** минимум 50 ГБ свободного места
- **Сеть:** доступ к локальной сети организации

### Что такое Ubuntu Server?
Ubuntu Server - это операционная система Linux, специально предназначенная для работы на серверах. Она бесплатна и широко используется в профессиональной среде.

---

## 🚀 ПОДГОТОВКА СЕРВЕРА

### Шаг 1: Подключение к серверу

Если вы работаете на Windows, используйте программу **PuTTY** или **Windows Terminal**. Если на Linux/Mac, используйте терминал.

**Пример подключения через SSH:**
```bash
ssh user@10.0.0.45
```
Где:
- `user` - имя пользователя на сервере
- `10.0.0.45` - IP адрес вашего сервера

**В Windows (PuTTY):**
1. Откройте PuTTY
2. В поле "Host Name" введите IP адрес сервера (например, `10.0.0.45`)
3. Убедитесь, что порт установлен на `22`
4. Нажмите "Open"
5. Введите логин и пароль когда попросит

### Шаг 2: Обновление системы

После подключения к серверу выполните следующие команды:

```bash
# Обновление списка пакетов
sudo apt update

# Обновление системы
sudo apt upgrade -y
```

**Что делают эти команды:**
- `sudo` - означает "выполнить от имени администратора"
- `apt update` - обновляет список доступных программ
- `apt upgrade` - устанавливает обновления для системы
- `-y` - автоматически отвечает "да" на все вопросы

**Примечание:** Вам может потребоваться ввести пароль администратора.

---

## 🐳 УСТАНОВКА DOCKER

### Что такое Docker?

Docker - это программа, которая позволяет "упаковать" приложение вместе со всеми его зависимостями в контейнер. Это позволяет легко запускать приложение на любом сервере без сложной настройки.

### Шаг 1: Установка Docker

Выполните следующие команды по порядку:

```bash
# Удаление старых версий Docker (если есть)
sudo apt remove docker docker-engine docker.io containerd runc -y

# Установка необходимых пакетов
sudo apt install -y ca-certificates curl gnupg lsb-release

# Добавление официального GPG ключа Docker
sudo mkdir -p /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg

# Добавление репозитория Docker
echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu \
  $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

# Обновление списка пакетов
sudo apt update

# Установка Docker Engine и Docker Compose
sudo apt install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin

# Запуск Docker
sudo systemctl start docker
sudo systemctl enable docker
```

**Что делают эти команды:**
- Устанавливают Docker и Docker Compose из официального репозитория
- `systemctl start docker` - запускает Docker
- `systemctl enable docker` - настраивает автоматический запуск Docker при загрузке сервера

### Шаг 2: Проверка установки

Проверьте, что Docker установлен правильно:

```bash
# Проверка версии Docker
docker --version

# Проверка версии Docker Compose
docker compose version

# Проверка работы Docker
sudo docker run hello-world
```

Если вы увидели сообщение "Hello from Docker!", значит всё установлено правильно!

### Шаг 3: Настройка прав доступа

Чтобы не использовать `sudo` перед каждой командой Docker, добавьте своего пользователя в группу docker:

```bash
# Добавление пользователя в группу docker
sudo usermod -aG docker $USER

# Применение изменений (нужно переподключиться к серверу)
newgrp docker

# Проверка (теперь должно работать без sudo)
docker ps
```

**Важно:** После выполнения `usermod` нужно переподключиться к серверу, чтобы изменения вступили в силу.

---

## 📦 РАЗВЕРТЫВАНИЕ ПРОЕКТА

### Шаг 1: Получение файлов проекта

У вас есть два варианта:

#### Вариант А: Использование готового бэкапа

Если у вас есть архив бэкапа проекта (файл `.tar.gz`):

```bash
# Создание директории для проекта
sudo mkdir -p /home/user/crm-system
sudo chown $USER:$USER /home/user/crm-system

# Перемещение архива на сервер (если он на другом компьютере)
# Используйте SCP или WinSCP для копирования файла

# Распаковка архива
cd /home/user
tar xzf crm-system-backup.tar.gz

# Переход в директорию проекта
cd crm-system
```

#### Вариант Б: Копирование файлов проекта

Если файлы проекта уже находятся на сервере:

```bash
# Переход в директорию проекта
cd /home/user/crm-system
```

### Шаг 2: Настройка переменных окружения

Создайте файл `.env` с настройками проекта:

```bash
# Копирование примера конфигурации
cp .env.example .env

# Редактирование файла
nano .env
```

**Минимальные настройки в файле `.env`:**

```env
# База данных
POSTGRES_USER=crm_user
POSTGRES_PASSWORD=ваш_надежный_пароль_здесь
POSTGRES_DB=crm_db

# Безопасность
SECRET_KEY=ваш_секретный_ключ_минимум_32_символа
BACKEND_CORS_ORIGINS=["http://10.0.0.45","http://localhost"]

# API URL (замените на IP вашего сервера)
VITE_API_URL=http://10.0.0.45:8000
```

**Как редактировать в nano:**
- `Ctrl + O` - сохранить файл
- `Ctrl + X` - выйти из редактора
- Стрелки вверх/вниз - перемещение по файлу

**Важно:** Обязательно измените пароли и секретный ключ на свои!

### Шаг 3: Запуск проекта

```bash
# Переход в директорию проекта
cd /home/user/crm-system

# Запуск всех контейнеров
docker compose up -d
```

**Что делает эта команда:**
- `docker compose up` - запускает все сервисы из файла `docker-compose.yml`
- `-d` - запускает в фоновом режиме (detached mode)

**Ожидание запуска (30-60 секунд):**
```bash
# Просмотр логов для проверки запуска
docker compose logs -f
```

Нажмите `Ctrl + C` чтобы выйти из просмотра логов.

### Шаг 4: Проверка статуса

```bash
# Проверка статуса всех контейнеров
docker compose ps
```

Вы должны увидеть статус "Up" для всех контейнеров:
- `crm_postgres` - база данных
- `crm_backend` - серверная часть
- `crm_frontend` - веб-интерфейс
- `crm_nginx` - веб-сервер

---

## ⚙️ ПЕРВОНАЧАЛЬНАЯ НАСТРОЙКА

### Шаг 1: Применение миграций базы данных

```bash
# Применение миграций
docker compose exec backend alembic upgrade head
```

**Что делает эта команда:**
- Создает все необходимые таблицы в базе данных
- Применяет все изменения структуры базы данных

### Шаг 2: Создание администратора

```bash
# Создание первого администратора
docker compose exec backend python scripts/create_admin.py
```

Вам будет предложено ввести:
- Email (будет использоваться для входа)
- Пароль (минимум 6 символов)
- Полное имя

**Пример:**
```
Email: admin@company.local
Password: Admin123!
Full Name: Главный Администратор
```

### Шаг 3: Проверка доступа

Откройте браузер и перейдите по адресу:

```
http://10.0.0.45
```

Замените `10.0.0.45` на IP адрес вашего сервера.

Вы должны увидеть страницу входа в систему. Войдите с учетными данными администратора, которые вы только что создали.

### Шаг 4: Настройка автоматического запуска при загрузке системы

Для автоматического запуска системы при перезагрузке сервера выполните:

```bash
# Переход в директорию проекта
cd /home/user/crm-system

# Запуск скрипта настройки автозапуска (требуются права root)
sudo ./setup_autostart.sh
```

После выполнения этого скрипта система будет автоматически запускаться при загрузке сервера.

**Управление сервисом:**
```bash
# Запустить систему
sudo systemctl start sup-system

# Остановить систему
sudo systemctl stop sup-system

# Перезапустить систему
sudo systemctl restart sup-system

# Проверить статус
sudo systemctl status sup-system
```

**Примечание:** Даже без настройки автозапуска контейнеры имеют параметр `restart: unless-stopped` в `docker-compose.yml`, что означает, что они автоматически перезапустятся при перезагрузке Docker daemon. Однако настройка systemd сервиса обеспечивает более надежный автоматический запуск.

---

## 📝 ПОВСЕДНЕВНЫЕ КОМАНДЫ

### Управление системой

#### Просмотр статуса системы
```bash
# Статус всех контейнеров
docker compose ps

# Проверка использования ресурсов
docker stats
```

#### Запуск системы
```bash
# Запуск всех сервисов
docker compose up -d
```

#### Остановка системы
```bash
# Остановка всех сервисов
docker compose down
```

#### Перезапуск системы
```bash
# Перезапуск всех сервисов
docker compose restart

# Перезапуск конкретного сервиса
docker compose restart backend
docker compose restart frontend
docker compose restart postgres
```

### Просмотр логов

#### Все логи
```bash
# Просмотр логов всех сервисов
docker compose logs -f
```

#### Логи конкретного сервиса
```bash
# Логи backend (серверная часть)
docker compose logs -f backend

# Логи frontend (веб-интерфейс)
docker compose logs -f frontend

# Логи базы данных
docker compose logs -f postgres

# Логи веб-сервера
docker compose logs -f nginx
```

**Что означает `-f`:**
- От слова "follow" - следить за логами в реальном времени
- Нажмите `Ctrl + C` чтобы выйти

#### Последние строки логов
```bash
# Последние 100 строк логов backend
docker compose logs --tail=100 backend

# Последние 50 строк всех логов
docker compose logs --tail=50
```

### Работа с базой данных

#### Создание бэкапа базы данных
```bash
# Бэкап в SQL формат
docker compose exec postgres pg_dump -U crm_user crm_db > backup_$(date +%Y%m%d_%H%M%S).sql

# Бэкап в бинарном формате
docker compose exec postgres pg_dump -U crm_user -F c -b crm_db > backup_$(date +%Y%m%d_%H%M%S).dump
```

#### Восстановление базы данных
```bash
# Восстановление из SQL файла
docker compose exec -T postgres psql -U crm_user crm_db < backup_file.sql
```

#### Подключение к базе данных
```bash
# Вход в консоль PostgreSQL
docker compose exec postgres psql -U crm_user crm_db

# Полезные команды внутри PostgreSQL:
# \dt - показать все таблицы
# \d table_name - показать структуру таблицы
# \q - выйти из консоли
```

### Обновление системы

```bash
# Остановка системы
docker compose down

# Обновление кода (замените файлы проекта на новые версии)

# Пересборка и запуск
docker compose up -d --build

# Применение новых миграций (если есть)
docker compose exec backend alembic upgrade head
```

### Очистка системы

```bash
# Удаление неиспользуемых образов
docker image prune -a

# Удаление остановленных контейнеров
docker container prune

# Полная очистка (осторожно!)
docker system prune -a --volumes
```

**Внимание:** Команда `docker system prune` удалит все неиспользуемые данные Docker. Используйте только если уверены!

---

## 🔧 РЕШЕНИЕ ПРОБЛЕМ

### Проблема: Контейнеры не запускаются

**Решение:**
```bash
# Просмотр подробных логов
docker compose logs

# Проверка статуса
docker compose ps

# Перезапуск
docker compose restart
```

### Проблема: Не могу зайти на сайт

**Проверьте:**

1. **Запущены ли контейнеры:**
   ```bash
   docker compose ps
   ```
   Все должны быть в статусе "Up"

2. **Открыты ли порты в firewall:**
   ```bash
   # Проверка открытых портов
   sudo ufw status
   
   # Если firewall включен, откройте порты:
   sudo ufw allow 80/tcp
   sudo ufw allow 443/tcp
   ```

3. **Правильный ли IP адрес:**
   ```bash
   # Узнать IP адрес сервера
   hostname -I
   ```

### Проблема: Ошибка подключения к базе данных

**Решение:**
```bash
# Проверка статуса PostgreSQL
docker compose logs postgres

# Перезапуск базы данных
docker compose restart postgres

# Проверка подключения
docker compose exec postgres psql -U crm_user -d crm_db -c "SELECT 1;"
```

### Проблема: Недостаточно места на диске

**Проверка:**
```bash
# Проверка свободного места
df -h

# Очистка старых логов и контейнеров
docker system prune -a
```

### Проблема: Ошибка при создании администратора

**Решение:**
```bash
# Проверка применения миграций
docker compose exec backend alembic upgrade head

# Повторная попытка создания администратора
docker compose exec backend python scripts/create_admin.py
```

---

## 💾 БЭКАП И ВОССТАНОВЛЕНИЕ

### Создание полного бэкапа

Используйте скрипт для создания полного бэкапа:

```bash
# Переход в директорию проекта
cd /home/user/crm-system

# Запуск скрипта бэкапа
./backup_project.sh
```

**Что создается:**
- Бэкап базы данных
- Бэкап всех файлов проекта
- Бэкап загруженных пользователями файлов
- Конфигурация Docker

**Где находится бэкап:**
```
/home/user/crm-system/backups/sup_backup_YYYYMMDD_HHMMSS.tar.gz
```

### Восстановление из бэкапа

```bash
# Переход в директорию проекта
cd /home/user/crm-system

# Запуск скрипта восстановления
./restore_project.sh backups/sup_backup_20251031_120000.tar.gz
```

Замените имя файла на имя вашего бэкапа.

### Ручное создание бэкапа

Если скрипт не работает, можно создать бэкап вручную:

```bash
# Бэкап базы данных
docker compose exec postgres pg_dump -U crm_user crm_db > db_backup.sql

# Бэкап файлов проекта
tar czf project_backup.tar.gz backend/ frontend/ nginx/ docker-compose.yml

# Бэкап загруженных файлов
tar czf uploads_backup.tar.gz backend/uploads/
```

---

## 🌐 ДОСТУП К СИСТЕМЕ

### Определение IP адреса сервера

```bash
# Показать все IP адреса
hostname -I

# Показать основной IP адрес
hostname -I | awk '{print $1}'
```

### Доступ через браузер

После запуска системы откройте браузер и перейдите по адресу:

```
http://IP_АДРЕС_СЕРВЕРА
```

Например:
```
http://10.0.0.45
```

### Доступ к API документации

```
http://IP_АДРЕС_СЕРВЕРА/api/docs
```

---

## 📞 ПОЛЕЗНЫЕ СОВЕТЫ

### Работа с командной строкой

- **Копирование текста:** В большинстве терминалов просто выделите текст мышью
- **Вставка текста:** `Ctrl + Shift + V` или правая кнопка мыши
- **Остановка команды:** `Ctrl + C`
- **Очистка экрана:** `clear` или `Ctrl + L`
- **История команд:** Стрелки вверх/вниз
- **Автодополнение:** Нажмите Tab

### Безопасность

1. **Регулярно создавайте бэкапы** - минимум раз в день
2. **Используйте надежные пароли** - минимум 12 символов
3. **Не делитесь паролями** - храните их в безопасном месте
4. **Регулярно обновляйте систему** - `sudo apt update && sudo apt upgrade`

### Производительность

- **Мониторинг ресурсов:** Используйте `docker stats` для просмотра использования ресурсов
- **Очистка:** Регулярно очищайте неиспользуемые Docker образы
- **Бэкапы:** Храните бэкапы на отдельном диске или сервере

---

## 📚 ДОПОЛНИТЕЛЬНАЯ ИНФОРМАЦИЯ

### Структура проекта

```
crm-system/
├── backend/          # Серверная часть (Python/FastAPI)
├── frontend/         # Веб-интерфейс (React)
├── nginx/           # Конфигурация веб-сервера
├── backups/         # Директория для бэкапов
├── docker-compose.yml  # Конфигурация Docker
├── backup_project.sh   # Скрипт создания бэкапа
└── restore_project.sh  # Скрипт восстановления
```

### Документация

- **README.md** - основная документация проекта
- **ADMIN_GUIDE.md** - руководство администратора
- **QUICK_START.md** - быстрый старт

### Поддержка

Если у вас возникли проблемы:

1. Проверьте логи: `docker compose logs`
2. Проверьте статус: `docker compose ps`
3. Посмотрите раздел "Решение проблем" выше
4. Проверьте документацию в папке проекта

---

## ✅ ЧЕКЛИСТ РАЗВЕРТЫВАНИЯ

Используйте этот чеклист для проверки правильности развертывания:

- [ ] Docker установлен и работает
- [ ] Docker Compose установлен и работает
- [ ] Файлы проекта скопированы на сервер
- [ ] Файл `.env` создан и настроен
- [ ] Все контейнеры запущены (`docker compose ps`)
- [ ] Миграции применены (`alembic upgrade head`)
- [ ] Администратор создан
- [ ] Система доступна через браузер
- [ ] Можно войти в систему

---

**Удачи в работе с системой СУП!** 🚀

Если у вас остались вопросы, обратитесь к документации или IT-специалисту вашей организации.

